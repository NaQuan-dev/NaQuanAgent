# Local Scripts

Real runtime scripts belong in the adopter's local private workspace and are not versioned in this framework repository.

Reusable script skeletons live in `templates/scripts/`. When a real external system is needed, copy the skeleton into a private workspace and implement the adapter and credential loading there.
